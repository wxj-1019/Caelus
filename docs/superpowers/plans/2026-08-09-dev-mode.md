# 开发模式：编译/调试时自动优化资源 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 检测编译/调试进程启动时自动压制后台 + 暂停索引服务,编译退出后自动恢复。

**Architecture:** 复用现有 `ProcNotify`(进程事件)→ `SuppressionCore`(后台压制)→ `SvcPause`(服务暂停)内核。新增三个小文件:`BuildCatalog`(编译进程名目录)、`DevServiceWhitelist`(开发服务豁免)、`BuildWatch`(状态机)。在 Program.cs 接线,设置页加开关。

**Tech Stack:** C# / .NET Framework 4.x / WinForms,`csc.exe` 直接编译。

**构建/测试命令:**
- 编译:`cmd.exe //c "build.cmd"`
- 自测:`cmd.exe //c "dev.cmd test"`(期望 `TOTAL 152+  PASS 149+  FAIL 0  SKIP 3`)

**关键接口(已核实):**
- `ProcessChange{Name, Kind(Started/Stopped), Pid}` — `src/Platform/ProcNotify.cs:13`
- `SuppressionCore.Acquire(pid, name, reason, group, level)` / `Release(pid, reason)` — `src/Core/Suppression/SuppressionCore.cs:217/395`
- `SuppressReason`:None=0, AntiCheat=1, Background=2 — `src/Core/Suppression/SuppressionCore.cs:11`(**需新增 Build=4**)
- `SuppressionLevel`:None=0, Eco=1, Restrained=2, Isolated=3, Frozen=4 — `BackgroundPressureController.cs:9`(用 Restrained 温和档)
- `SvcPause.Activate()` / `Restore()` — 幂等,有 `active` 标志,已暂停 SysMain+WSearch — `src/Core/Tweaks/SvcPause.cs:17/73`
- `Settings.Load/Save(name, bool)` — `src/Platform/Settings.cs`

---

### Task 1: 新增 SuppressReason.Build 枚举值

**Files:**
- Modify: `src/Core/Suppression/SuppressionCore.cs:11`

- [ ] **Step 1: 加 Build 枚举值**

把:
```csharp
    internal enum SuppressReason
    {
        None = 0,
        AntiCheat = 1,
        Background = 2
    }
```
改为:
```csharp
    internal enum SuppressReason
    {
        None = 0,
        AntiCheat = 1,
        Background = 2,
        Build = 4
    }
```

- [ ] **Step 2: 编译验证**

`cmd.exe //c "build.cmd"` → `Build OK`

- [ ] **Step 3: 暂不提交**

---

### Task 2: 新建 BuildCatalog.cs — 编译/调试进程名目录

**Files:**
- Create: `src/Core/BuildCatalog.cs`

- [ ] **Step 1: 创建文件**

```csharp
// @author zenjiro 18967498922@163.com
// 文件用途 识别编译/调试/构建进程，作为开发模式的触发信号

using System;
using System.Collections.Generic;

namespace CaelusApp
{
    internal static class BuildCatalog
    {
        // 明确的编译/构建/调试工具进程名（不含运行时如 node/java/dotnet，避免误触发）
        private static readonly HashSet<string> Names = new HashSet<string>(StringComparer.OrdinalIgnoreCase)
        {
            // .NET
            "msbuild", "csc", "vbc", "fsc", "roslyn", "msbuildsdp", "dotnet-build",
            // JVM
            "javac", "gradle", "mvn", "sbt", "kotlinc", "kotlin-compiler",
            // C/C++/Rust
            "gcc", "g++", "clang", "clang++", "cmake", "make", "ninja", "ld", "lld", "lld-link", "rustc", "cargo", "x86_64-w64-mingw32-g++",
            // Node/前端
            "tsc", "webpack", "vite", "esbuild", "rollup", "gulp", "babel", "swc", "parcel", "turbo",
            // 调试器
            "gdb", "lldb", "msvsmon", "remote-debugger"
        };

        public static bool IsMatch(string processName)
        {
            if (string.IsNullOrEmpty(processName)) return false;
            string bare = processName.EndsWith(".exe", StringComparison.OrdinalIgnoreCase)
                ? processName.Substring(0, processName.Length - 4) : processName;
            return Names.Contains(bare);
        }
    }
}
```

- [ ] **Step 2: 编译验证**

`cmd.exe //c "build.cmd"` → `Build OK`

- [ ] **Step 3: 暂不提交**

---

### Task 3: 新建 DevServiceWhitelist.cs — 开发服务豁免目录

**Files:**
- Create: `src/Core/DevServiceWhitelist.cs`

- [ ] **Step 1: 创建文件**

```csharp
// @author zenjiro 18967498922@163.com
// 文件用途 编译期间不压的开发常驻服务进程名

using System;
using System.Collections.Generic;

namespace CaelusApp
{
    internal static class DevServiceWhitelist
    {
        private static readonly HashSet<string> Names = new HashSet<string>(StringComparer.OrdinalIgnoreCase)
        {
            // Docker
            "docker", "dockerd", "com.docker.backend", "com.docker.service", "vpnkit", "docker-proxy", "containerd",
            // 数据库
            "postgres", "mysqld", "mariadbd", "redis-server", "redis-server.exe", "mongod", "sqlservr", "oracle", "sqlite3",
            // ES/日志栈
            "elasticsearch", "kibana", "logstash", "filebeat", "metricbeat",
            // 消息队列
            "rabbitmq-server", "kafka", "nats-server"
        };

        public static bool Contains(string processName)
        {
            if (string.IsNullOrEmpty(processName)) return false;
            string bare = processName.EndsWith(".exe", StringComparison.OrdinalIgnoreCase)
                ? processName.Substring(0, processName.Length - 4) : processName;
            return Names.Contains(bare);
        }
    }
}
```

- [ ] **Step 2: 编译验证**

`cmd.exe //c "build.cmd"` → `Build OK`

- [ ] **Step 3: 暂不提交**

---

### Task 4: 新建 BuildWatch.cs — 编译会话状态机

核心状态机。管理活跃编译进程集合,激活/恢复压制。

**Files:**
- Create: `src/Core/BuildWatch.cs`

- [ ] **Step 1: 创建文件**

```csharp
// @author zenjiro 18967498922@163.com
// 文件用途 检测编译/调试进程的启动与退出，自动压制后台并暂停索引服务

using System;
using System.Collections.Generic;
using System.Diagnostics;

namespace CaelusApp
{
    internal sealed class BuildWatch
    {
        private readonly SuppressionCore core;
        private readonly object sync = new object();
        private readonly HashSet<int> activeBuildPids = new HashSet<int>();
        private bool suppressing;
        private bool enabled = true;

        public bool Enabled
        {
            get { lock (sync) return enabled; }
            set { lock (sync) enabled = value; }
        }

        public bool IsActive { get { lock (sync) return activeBuildPids.Count > 0; } }

        /// <summary>编译会话状态变化时触发，参数是气泡消息（null=无变化）</summary>
        public event Action<string> SessionChanged;

        public BuildWatch(SuppressionCore core)
        {
            this.core = core;
        }

        public void NotifyProcessChanges(ProcessChangeBatch batch)
        {
            if (batch == null || batch.Changes == null) return;
            bool becameActive = false;
            bool becameIdle = false;
            int activeCount = 0;

            lock (sync)
            {
                if (!enabled) return;

                foreach (ProcessChange pc in batch.Changes)
                {
                    if (string.IsNullOrEmpty(pc.Name)) continue;
                    if (!BuildCatalog.IsMatch(pc.Name)) continue;

                    if (pc.Kind == ProcessChangeKind.Started)
                    {
                        if (activeBuildPids.Add(pc.Pid) && !suppressing)
                            becameActive = true;
                    }
                    else if (pc.Kind == ProcessChangeKind.Stopped)
                    {
                        activeBuildPids.Remove(pc.Pid);
                    }
                }

                activeCount = activeBuildPids.Count;

                if (activeCount > 0 && !suppressing)
                {
                    suppressing = true;
                    becameActive = true;
                }
                else if (activeCount == 0 && suppressing)
                {
                    suppressing = false;
                    becameIdle = true;
                }
            }

            // 锁外执行副作用（耗时操作不持锁）
            if (becameActive) ActivateSuppression(activeCount);
            if (becameIdle) DeactivateSuppression();
        }

        private void ActivateSuppression(int activeCount)
        {
            try
            {
                // 暂停索引/预取服务（幂等）
                SvcPause.Activate();
                // 编译进程自身提优
                BoostBuildProcesses();
                Logger.Log("开发模式：检测到编译/调试进程（" + activeCount + " 个活跃），已压制后台并暂停索引服务");
                try { SessionChanged("bal.buildstart"); } catch { }
            }
            catch (Exception ex) { Logger.LogFailure("开发模式激活失败", ex); }
        }

        private void DeactivateSuppression()
        {
            try
            {
                SvcPause.Restore();
                Logger.Log("开发模式：编译/调试进程已退出，恢复后台资源");
                try { SessionChanged("bal.buildend"); } catch { }
            }
            catch (Exception ex) { Logger.LogFailure("开发模式恢复失败", ex); }
        }

        private void BoostBuildProcesses()
        {
            int[] pids;
            lock (sync)
            {
                pids = new int[activeBuildPids.Count];
                activeBuildPids.CopyTo(pids);
            }
            foreach (int pid in pids)
            {
                try
                {
                    // 编译进程提优：用 Acquire 的 Background reason 反向操作不合适，
                    // 直接设高优先级 + 高 IO
                    IntPtr h = Native.OpenProcess(Native.PROCESS_SET_INFORMATION, false, pid);
                    if (h == IntPtr.Zero) continue;
                    try
                    {
                        Native.SetPriorityClass(h, Native.PriorityHigh);
                        Native.TrySetIoPriority(h, 3);
                    }
                    finally { Native.CloseHandle(h); }
                }
                catch { }
            }
        }

        /// <summary>程序退出时调用，确保恢复</summary>
        public void Stop()
        {
            lock (sync)
            {
                if (!suppressing) return;
                suppressing = false;
                activeBuildPids.Clear();
            }
            DeactivateSuppression();
        }
    }
}
```

注意:需核实 `Native.PriorityHigh`、`Native.SetPriorityClass`、`Native.TrySetIoPriority`、`Native.PROCESS_SET_INFORMATION` 的确切名称(见 Step 2)。

- [ ] **Step 2: 核实 Native 常量名**

运行:`grep -n "PriorityHigh\|ABOVE_NORMAL\|SetPriorityClass\|PROCESS_SET_INFORMATION\|TrySetIoPriority" src/Platform/Native.cs`

如名称不同,按实际名称调整 BoostBuildProcesses 的调用。典型情况:
- `Native.PROCESS_SET_INFORMATION` 可能叫 `PROCESS_SET_INFORMATION`
- `Native.SetPriorityClass` 可能需两个参数 `(handle, uint)`
- `Native.TrySetIoPriority(handle, int)` 可能签名不同

- [ ] **Step 3: 编译验证**

`cmd.exe //c "build.cmd"` → `Build OK`(若 Native 调用名不对,编译会报错,按报错修正)

- [ ] **Step 4: 暂不提交**

---

### Task 5: Program.cs 接线

把 BuildWatch 接入进程事件流和托盘气泡。

**Files:**
- Modify: `src/Program.cs`

- [ ] **Step 1: 创建 BuildWatch 实例**

在 `var gameMode = new GameMode(...)` 之后(约 Program.cs:261 附近)加:

```csharp
            var buildWatch = new BuildWatch(core);
            buildWatch.Enabled = Settings.Load("DevModeOn", true);
```

- [ ] **Step 2: 接入进程事件**

在 `procNotify.BatchChanged += batch =>` 回调里(约 Program.cs:294)加一行:

```csharp
            procNotify.BatchChanged += batch =>
            {
                gameMode.NotifyProcessChanges(batch);
                tamer.NotifyProcessChanges(batch);
                buildWatch.NotifyProcessChanges(batch);
            };
```

- [ ] **Step 3: 托盘气泡订阅**

在 `gameMode.GameAutoAdded += name =>` 块之后(约 Program.cs:374),加气泡订阅:

```csharp
            buildWatch.SessionChanged += key =>
            {
                try
                {
                    panel.BeginInvoke((MethodInvoker)(() =>
                    {
                        try { icon.ShowBalloonTip(5000, App.DisplayName, Lang.T(key), ToolTipIcon.Info); } catch { }
                    }));
                }
                catch { }
            };
```

- [ ] **Step 4: 退出时恢复**

在 `doExit` lambda 里(约 Program.cs:337-349),`gameMode.Stop()` 之后加:

```csharp
                buildWatch.Stop();
```

- [ ] **Step 5: 注销/关机恢复**

在 `SystemEvents.SessionEnded` 回调里(约 Program.cs:346),加:

```csharp
                try { buildWatch.Stop(); } catch { }
```

- [ ] **Step 6: 编译验证**

`cmd.exe //c "build.cmd"` → `Build OK`

- [ ] **Step 7: 暂不提交**

---

### Task 6: Lang.cs 新增文案

**Files:**
- Modify: `src/Platform/Lang.cs`

- [ ] **Step 1: 加文案**

在 Lang.cs 文案字典里(找 `bal.update` 附近)加:

```csharp
            { "bal.buildstart", new[]{ "编译优化中：后台已降级，编译器优先拿资源" } },
            { "bal.buildend", new[]{ "编译结束，已恢复后台资源" } },
            { "set.dev", new[]{ "开发模式" } },
            { "set.dev.n", new[]{ "检测到编译/调试进程（msbuild、gcc、npm 等）时自动压制后台，编译完恢复" } },
```

- [ ] **Step 2: 编译 + 自测**

`cmd.exe //c "dev.cmd test"` → `TOTAL 152  PASS 149  FAIL 0  SKIP 3`

- [ ] **Step 3: 暂不提交**

---

### Task 7: 设置页新增开发模式开关

**Files:**
- Modify: `src/Ui/Pages/PanelForm.SettingsPage.cs`

- [ ] **Step 1: 加字段声明**

在 `private Toggle swAuto, swAutoHide;`(约 :14)后加:
```csharp
        private Toggle swDev;
```

- [ ] **Step 2: 加开关卡片**

在"自动隐藏"卡片之后(约 :40 的 `sy += cardH + 8;` 后)、`sy += 10;` 之前,加:

```csharp
            swDev = MakeSwitch(Settings.Load("DevModeOn", true), delegate
            {
                Settings.Save("DevModeOn", swDev.Checked);
            });
            MakeAutoCard(scroll, 6, sy, ScrollContentW, 76, Lang.T("set.dev"), Lang.T("set.dev.n"), swDev, out cardH);
            sy += cardH + 8;
```

注意:`swDev.Checked` 改变时只写设置;实际 BuildWatch.Enabled 的更新需要通过 Program.cs 传入。最简方案:在 Step 2 的 delegate 里加一行调用 Program.cs 暴露的静态引用,或让 BuildWatch 每次检查设置。更简单:delegate 只存设置,BuildWatch 在 NotifyProcessChanges 开头读 `Settings.Load("DevModeOn", true)`。

修正 BuildWatch.Enabled 逻辑:改为在 NotifyProcessChanges 里读设置而非用属性:
```csharp
                if (!Settings.Load("DevModeOn", true)) return;
```
(删除 enabled 字段和 Enabled 属性,Task 4 对应处也改)

- [ ] **Step 3: 编译 + 自测**

`cmd.exe //c "dev.cmd test"` → `TOTAL 152  PASS 149  FAIL 0  SKIP 3`

---

### Task 8: 最终验证 + 提交

- [ ] **Step 1: 全量编译 + 自测**

`cmd.exe //c "dev.cmd test"` → `TOTAL 152  PASS 149  FAIL 0  SKIP 3`

- [ ] **Step 2: 手动验证(启动一个编译进程)**

构建发布版,启动 Caelus,然后在终端运行一个编译命令(如 `tsc --version` 触发 tsc 进程,或 `cmd.exe //c "msbuild -version"`),观察托盘气泡是否提示"编译优化中"。

注意:短命进程(如 `tsc --version` 瞬间退出)可能气泡一闪而过。用长编译任务更明显。

- [ ] **Step 3: 提交**

```bash
git add -A
git commit -m "feat: 开发模式——编译/调试时自动压制后台并暂停索引服务

- 新增 BuildCatalog：识别 .NET/JVM/C++/Node/调试器编译进程
- 新增 DevServiceWhitelist：编译期间不压 Docker/数据库/ES
- 新增 BuildWatch：状态机，检测编译进程启停，自动激活/恢复压制
- 复用 SuppressionCore（温和档降级）+ SvcPause（暂停索引服务）
- 设置页新增开发模式开关，编译期间托盘气泡提示
- 编译进程自身提优（高 CPU + 高 IO 优先级）"
```
