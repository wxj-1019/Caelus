<UserControl x:Class="CaelusApp.WpfHost.Controls.SegmentedControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root"
             PreviewKeyDown="OnPreviewKeyDown">
  <UserControl.Resources>
    <!-- SegmentItem supplies the shared metrics and states; this template leaves the
         selected fill to the single sliding indicator behind all item controls. -->
    <ControlTemplate x:Key="SlidingSegmentItemTemplate" TargetType="{x:Type RadioButton}">
      <Border x:Name="SegmentBody"
              CornerRadius="{DynamicResource RadiusSm}"
              Padding="{TemplateBinding Padding}"
              Background="Transparent">
        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
      </Border>
      <ControlTemplate.Triggers>
        <Trigger Property="IsChecked" Value="True">
          <Setter Property="Foreground" Value="{DynamicResource ModeAccentBrush}"/>
          <Setter Property="FontWeight" Value="SemiBold"/>
        </Trigger>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter TargetName="SegmentBody" Property="Opacity" Value="0.9"/>
        </Trigger>
      </ControlTemplate.Triggers>
    </ControlTemplate>
  </UserControl.Resources>

  <Border Style="{DynamicResource SegmentHost}">
    <Grid x:Name="LayoutHost" ClipToBounds="True">
      <Border x:Name="SelectionIndicator"
              HorizontalAlignment="Left"
              VerticalAlignment="Stretch"
              Background="{DynamicResource AccentSoftBrush}"
              CornerRadius="{DynamicResource RadiusSm}"
              IsHitTestVisible="False"
              Visibility="Hidden">
        <Border.RenderTransform>
          <TranslateTransform x:Name="IndicatorTranslate"/>
        </Border.RenderTransform>
      </Border>

      <ItemsControl x:Name="ItemsHost">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <StackPanel Orientation="Horizontal"/>
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
      </ItemsControl>
    </Grid>
  </Border>
</UserControl>
