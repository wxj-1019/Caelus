<Window x:Class="CaelusApp.WpfHost.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:CaelusApp.WpfHost.Controls"
        Title="Caelus" Width="1196" Height="768" MinWidth="980" MinHeight="640"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="False" ResizeMode="CanResize"
        Background="{DynamicResource BackgroundBrush}"
        FontFamily="{DynamicResource FontUi}">
  <!-- 窗口圆角由 DWM 提供（DWMWA_WINDOW_CORNER_PREFERENCE=ROUND，见 OnSourceInitialized）：
       系统级裁剪无直角残留、最大化自动方角、保留系统阴影、四角无点击穿透。
       需 AllowsTransparency=False 才能让 DWM 圆角与 Mica 材质（DWMSBT_MAINWINDOW）同时生效。 -->
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="36"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource BorderSubtleBrush}" BorderThickness="0,0,0,1"
            MouseLeftButtonDown="TitleBarDrag">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- WPF PREVIEW 徽章（左上角） -->
        <Border Grid.Column="0" Margin="12,0,0,0" Padding="6,2" VerticalAlignment="Center"
                CornerRadius="{DynamicResource RadiusSm}"
                Background="{DynamicResource Surface2Brush}"
                BorderBrush="{DynamicResource BorderSubtleBrush}" BorderThickness="1">
          <TextBlock Text="WPF PREVIEW" FontSize="{DynamicResource FontSizeMicro}" Foreground="{DynamicResource TextTertiaryBrush}"/>
        </Border>

        <!-- 居中窗口标题 -->
        <TextBlock Grid.Column="1" Text="C A E L U S" HorizontalAlignment="Center"
                   VerticalAlignment="Center" FontSize="{DynamicResource FontSizeSmall}" FontWeight="SemiBold"
                   Foreground="{DynamicResource TextSecondaryBrush}"/>

        <!-- 最小化/最大化/关闭（Windows 11 风格：悬停淡底，关闭悬停变红） -->
        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
          <Button x:Name="MinBtn" Width="46" Style="{DynamicResource WindowButton}"
                  AutomationProperties.Name="最小化窗口" Click="MinClick">
            <Path Width="12" Height="12" Stretch="Uniform" StrokeThickness="1"
                  Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                  Data="M1,6 L11,6"/>
          </Button>
          <Button x:Name="MaxBtn" Width="46" Style="{DynamicResource WindowButton}"
                  AutomationProperties.Name="最大化或还原窗口" Click="MaxClick">
            <Grid Width="12" Height="12">
              <Path x:Name="MaxIcon" Stretch="Uniform" StrokeThickness="1"
                    Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                    Data="M2,2 H10 V10 H2 Z"/>
              <Path x:Name="RestoreIcon" Stretch="Uniform" StrokeThickness="1"
                    Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                    Data="M4.5,1.5 H10 V7.5 M1.5,4.5 H9.5 V10.5 H1.5 Z"
                    Visibility="Collapsed"/>
            </Grid>
          </Button>
          <Button x:Name="CloseBtn" Width="46" Style="{DynamicResource CloseWindowButton}"
                  AutomationProperties.Name="关闭窗口" Click="CloseClick">
            <Path Width="12" Height="12" Stretch="Uniform" StrokeThickness="1"
                  Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                  Data="M1.5,1.5 L10.5,10.5 M10.5,1.5 L1.5,10.5"/>
          </Button>
        </StackPanel>
      </Grid>
    </Border>

    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="200"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>

      <Border Grid.Column="0" Background="{DynamicResource SurfaceBrush}"
              BorderBrush="{DynamicResource BorderSubtleBrush}" BorderThickness="0,0,1,0">
        <DockPanel LastChildFill="True">
          <StackPanel DockPanel.Dock="Top" Margin="18,18,18,10">
            <TextBlock Text="caelus" FontSize="{DynamicResource FontSizeTitle}" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}"/>
            <TextBlock Text="三场景调度控制台" FontSize="{DynamicResource FontSizeXs}" Margin="0,3,0,0"
                       Foreground="{DynamicResource TextTertiaryBrush}"/>
          </StackPanel>

          <StackPanel DockPanel.Dock="Bottom" Margin="0,8,0,10">
            <RadioButton x:Name="NavSettings" Style="{DynamicResource NavItem}" GroupName="nav"
                         Checked="NavChecked" AutomationProperties.Name="导航：设置">
              <StackPanel Orientation="Horizontal">
                <controls:IconView Key="IconSettings" Width="15" Height="15" VerticalAlignment="Center"/>
                <TextBlock Text="设置" Margin="9,0,0,0" VerticalAlignment="Center"/>
              </StackPanel>
            </RadioButton>
            <RadioButton x:Name="NavAbout" Style="{DynamicResource NavItem}" GroupName="nav"
                         Checked="NavChecked" AutomationProperties.Name="导航：关于">
              <StackPanel Orientation="Horizontal">
                <controls:IconView Key="IconInfo" Width="15" Height="15" VerticalAlignment="Center"/>
                <TextBlock Text="关于" Margin="9,0,0,0" VerticalAlignment="Center"/>
              </StackPanel>
            </RadioButton>
          </StackPanel>

          <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="0,0,0,8">
              <TextBlock Text="场景" Style="{DynamicResource NavGroupLabel}"/>
              <RadioButton x:Name="NavOverview" Style="{DynamicResource NavItem}" IsChecked="True"
                           GroupName="nav" Checked="NavChecked" AutomationProperties.Name="导航：场景总览">
                <StackPanel Orientation="Horizontal">
                  <controls:IconView Key="IconOverview" Width="15" Height="15" VerticalAlignment="Center"/>
                  <TextBlock Text="场景总览" Margin="9,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
              </RadioButton>

              <TextBlock Text="游戏场景" Style="{DynamicResource NavGroupLabel}"/>
              <RadioButton x:Name="NavLibrary" Style="{DynamicResource NavItem}" GroupName="nav"
                           Checked="NavChecked" AutomationProperties.Name="导航：游戏库">
                <StackPanel Orientation="Horizontal">
                  <controls:IconView Key="IconLibrary" Width="15" Height="15" VerticalAlignment="Center"/>
                  <TextBlock Text="游戏库" Margin="9,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
              </RadioButton>
              <RadioButton x:Name="NavPolicy" Style="{DynamicResource NavItem}" GroupName="nav"
                           Checked="NavChecked" AutomationProperties.Name="导航：优化策略">
                <StackPanel Orientation="Horizontal">
                  <controls:IconView Key="IconPolicy" Width="15" Height="15" VerticalAlignment="Center"/>
                  <TextBlock Text="优化策略" Margin="9,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
              </RadioButton>
              <RadioButton x:Name="NavGraphics" Style="{DynamicResource NavItem}" GroupName="nav"
                           Checked="NavChecked" AutomationProperties.Name="导航：显卡">
                <StackPanel Orientation="Horizontal">
                  <controls:IconView Key="IconGpu" Width="15" Height="15" VerticalAlignment="Center"/>
                  <TextBlock Text="显卡" Margin="9,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
              </RadioButton>
              <RadioButton x:Name="NavAntiCheat" Style="{DynamicResource NavItem}" GroupName="nav"
                           Checked="NavChecked" AutomationProperties.Name="导航：反作弊专项">
                <StackPanel Orientation="Horizontal">
                  <controls:IconView Key="IconShield" Width="15" Height="15" VerticalAlignment="Center"/>
                  <TextBlock Text="反作弊专项" Margin="9,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
              </RadioButton>

              <TextBlock Text="开发与日常" Style="{DynamicResource NavGroupLabel}"/>
              <RadioButton x:Name="NavDevFocus" Style="{DynamicResource NavItem}" GroupName="nav"
                           Checked="NavChecked" AutomationProperties.Name="导航：开发专注">
                <StackPanel Orientation="Horizontal">
                  <controls:IconView Key="IconCode" Width="15" Height="15" VerticalAlignment="Center"/>
                  <TextBlock Text="开发专注" Margin="9,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
              </RadioButton>
              <RadioButton x:Name="NavDailyCare" Style="{DynamicResource NavItem}" GroupName="nav"
                           Checked="NavChecked" AutomationProperties.Name="导航：日常优化">
                <StackPanel Orientation="Horizontal">
                  <controls:IconView Key="IconDaily" Width="15" Height="15" VerticalAlignment="Center"/>
                  <TextBlock Text="日常优化" Margin="9,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
              </RadioButton>

              <TextBlock Text="系统" Style="{DynamicResource NavGroupLabel}"/>
              <RadioButton x:Name="NavEnvironment" Style="{DynamicResource NavItem}" GroupName="nav"
                           Checked="NavChecked" AutomationProperties.Name="导航：系统环境">
                <StackPanel Orientation="Horizontal">
                  <controls:IconView Key="IconEnvironment" Width="15" Height="15" VerticalAlignment="Center"/>
                  <TextBlock Text="系统环境" Margin="9,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
              </RadioButton>
              <RadioButton x:Name="NavWhitelist" Style="{DynamicResource NavItem}" GroupName="nav"
                           Checked="NavChecked" AutomationProperties.Name="导航：白名单">
                <StackPanel Orientation="Horizontal">
                  <controls:IconView Key="IconWhitelist" Width="15" Height="15" VerticalAlignment="Center"/>
                  <TextBlock Text="白名单" Margin="9,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
              </RadioButton>

              <TextBlock Text="诊断" Style="{DynamicResource NavGroupLabel}"/>
              <RadioButton x:Name="NavAudit" Style="{DynamicResource NavItem}" GroupName="nav"
                           Checked="NavChecked" AutomationProperties.Name="导航：系统体检">
                <StackPanel Orientation="Horizontal">
                  <controls:IconView Key="IconAudit" Width="15" Height="15" VerticalAlignment="Center"/>
                  <TextBlock Text="系统体检" Margin="9,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
              </RadioButton>
              <RadioButton x:Name="NavLog" Style="{DynamicResource NavItem}" GroupName="nav"
                           Checked="NavChecked" AutomationProperties.Name="导航：日志">
                <StackPanel Orientation="Horizontal">
                  <controls:IconView Key="IconLog" Width="15" Height="15" VerticalAlignment="Center"/>
                  <TextBlock Text="日志" Margin="9,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
              </RadioButton>
            </StackPanel>
          </ScrollViewer>
        </DockPanel>
      </Border>

      <Grid Grid.Column="1" Margin="28,24,28,24">
        <ContentControl x:Name="PageHost" HorizontalContentAlignment="Stretch"
                        VerticalContentAlignment="Stretch"/>
      </Grid>
    </Grid>

  </Grid>
</Window>
