<UserControl x:Class="CaelusApp.WpfHost.Views.EnvironmentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
    <StackPanel Margin="0,0,0,16">

      <StackPanel x:Name="ZoneHeader" Margin="0,0,0,18">
        <TextBlock Text="{Binding PageTitle}" Style="{DynamicResource PageHeader}"/>
        <TextBlock Text="{Binding PageSub}" Style="{DynamicResource PageSubtitle}" Margin="0,3,0,0"/>
      </StackPanel>

      <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
        <Border Width="30" Height="30" CornerRadius="{DynamicResource RadiusSm}" Background="{DynamicResource AccentSoftBrush}" BorderBrush="{DynamicResource AccentEdgeBrush}" BorderThickness="1" VerticalAlignment="Center" Margin="0,0,12,0">
          <Viewbox Width="16" Height="16"><Path Data="M9 12a3 3 0 1 1 6 0 3 3 0 1 1-6 0 M12 2v3 M12 19v3 M2 12h3 M19 12h3" Stroke="{DynamicResource ModeAccentBrush}" StrokeThickness="2" StrokeLineJoin="Round"/></Viewbox>
        </Border>
        <TextBlock Text="{Binding KernelSection}" FontSize="{DynamicResource FontSizeBody}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
      </StackPanel>

      <Border Style="{DynamicResource SettingsGroup}">
      <ItemsControl x:Name="ToggleList" ItemsSource="{Binding Toggles}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate><StackPanel/></ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
              <Border Style="{DynamicResource PolicyRow}">
              <DockPanel>
                <ToggleButton Style="{DynamicResource PolicyToggle}"
                              IsChecked="{Binding IsOn, Mode=OneWay}"
                              Click="OnToggleClick"
                              AutomationProperties.Name="{Binding Title}"
                              DockPanel.Dock="Right" VerticalAlignment="Center" Margin="16,0,0,0"/>
                <StackPanel VerticalAlignment="Center">
                  <TextBlock Text="{Binding Title}" FontSize="{DynamicResource FontSizeCaption}"
                             FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"
                             TextWrapping="Wrap"/>
                  <TextBlock Text="{Binding Desc}" FontSize="11"
                             Foreground="{DynamicResource TextSecondaryBrush}"
                             TextWrapping="Wrap" Margin="0,2,0,0"/>
                </StackPanel>
              </DockPanel>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
      </Border>

    </StackPanel>
  </ScrollViewer>
</UserControl>
