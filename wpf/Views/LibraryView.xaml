<UserControl x:Class="CaelusApp.WpfHost.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             AllowDrop="True" Drop="OnDrop" DragOver="OnDragOver">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <!-- 页头（规格 §7 5a） -->
    <StackPanel Grid.Row="0" Margin="0,0,0,12">
      <TextBlock Text="游戏库" Style="{DynamicResource SectionTitle}"/>
      <TextBlock Text="已纳管的游戏与进程证据" Style="{DynamicResource SectionSubtitle}" Margin="0,3,0,0"/>
    </StackPanel>

    <!-- 空态（默认可见，code-behind 控制） -->
    <StackPanel x:Name="EmptyPanel" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
      <TextBlock Text="{Binding EmptyTitle}" FontSize="{DynamicResource FontSizeDisplay}"
                 FontWeight="Bold" Foreground="{DynamicResource TextTertiaryBrush}"
                 HorizontalAlignment="Center" Margin="0,0,0,8"/>
      <TextBlock Text="{Binding EmptyHint}" FontSize="{DynamicResource FontSizeCaption}"
                 Foreground="{DynamicResource TextSecondaryBrush}"
                 HorizontalAlignment="Center" TextAlignment="Center"
                 TextWrapping="Wrap" MaxWidth="400"/>
    </StackPanel>

    <!-- 列表（默认隐藏，code-behind 控制） -->
    <DockPanel x:Name="ListPanel" Grid.Row="1" Visibility="Collapsed">
      <!-- 顶部按钮栏 -->
      <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Style="{DynamicResource PrimaryButton}" x:Name="BtnAdd"
                Content="{Binding AddButtonText}" Click="OnAddClick" Margin="0,0,8,0"/>
        <Button Grid.Column="1" Style="{DynamicResource PrimaryButton}" x:Name="BtnRemove"
                Content="{Binding RemoveButtonText}" Click="OnRemoveClick" Opacity="0.7"/>
        <TextBlock Grid.Column="2" Text="{Binding DropHint}" FontSize="11"
                   Foreground="{DynamicResource TextTertiaryBrush}" TextWrapping="Wrap"
                   VerticalAlignment="Center" Margin="12,0,0,0" MaxWidth="500"/>
      </Grid>

      <!-- 游戏列表 -->
      <ScrollViewer VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding Items}">
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate><StackPanel/></ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <Border Style="{DynamicResource LibraryItemCard}" Margin="0,0,0,6">
                <DockPanel>
                  <!-- 运行状态徽标 -->
                  <TextBlock DockPanel.Dock="Right" VerticalAlignment="Center" FontSize="11">
                    <TextBlock.Style>
                      <Style TargetType="TextBlock">
                        <Setter Property="Text" Value="{Binding StatusText}"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                        <Style.Triggers>
                          <DataTrigger Binding="{Binding IsRunning}" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource SuccessBrush}"/>
                          </DataTrigger>
                        </Style.Triggers>
                      </Style>
                    </TextBlock.Style>
                  </TextBlock>
                  <!-- 名称 + 路径 -->
                  <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding Name}" FontSize="{DynamicResource FontSizeCaption}"
                               FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock Text="{Binding Path}" FontSize="11"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                  </StackPanel>
                </DockPanel>
              </Border>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </ScrollViewer>
    </DockPanel>
  </Grid>
</UserControl>
