<UserControl x:Class="CaelusApp.WpfHost.Views.OverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CaelusApp.WpfHost">
  <UserControl.Resources>
    <local:KeyBrushConverter x:Key="KeyBrush"/>
    <local:KeySoftBrushConverter x:Key="KeySoftBrush"/>
    <local:KeyColorConverter x:Key="KeyColor"/>
    <BooleanToVisibilityConverter x:Key="BoolVis"/>
    <local:DetailButtonTextConverter x:Key="DetailText"/>
    <local:FractionGridLengthConverter x:Key="FracLen"/>
  </UserControl.Resources>
  <StackPanel>
    <!-- 结论卡片（规格 §5.1：结论前置）——本环境 DropShadowEffect 不渲染，层级靠边框+填充 -->
    <Border Background="{DynamicResource GlassHeroBrush}"
            BorderBrush="{DynamicResource GlassBorderHiBrush}"
            BorderThickness="1" CornerRadius="{DynamicResource RadiusMd}" Padding="16,14">
      <DockPanel>
        <Border Width="32" Height="32" CornerRadius="16" VerticalAlignment="Center"
                Background="{Binding ConclusionColorKey, Converter={StaticResource KeySoftBrush}}">
          <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="0" Opacity="0.35"
                              Color="{Binding ConclusionColorKey, Converter={StaticResource KeyColor}}"/>
          </Border.Effect>
          <TextBlock Text="{Binding ConclusionGlyph}" HorizontalAlignment="Center"
                     VerticalAlignment="Center" FontSize="15"
                     Foreground="{Binding ConclusionColorKey, Converter={StaticResource KeyBrush}}"/>
        </Border>
        <Button Style="{DynamicResource GhostButton}" DockPanel.Dock="Right"
                VerticalAlignment="Center"
                AutomationProperties.Name="查看或收起诊断详情"
                Content="{Binding DetailVisible, Converter={StaticResource DetailText}}"
                Command="{Binding ToggleDetailCommand}"/>
        <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
          <TextBlock Text="{Binding ConclusionTitle}"
                     FontSize="{DynamicResource FontSizeTitle}" FontWeight="Bold"
                     Foreground="{DynamicResource TextPrimaryBrush}"/>
          <TextBlock Text="{Binding ConclusionDetail}"
                     FontSize="{DynamicResource FontSizeCaption}"
                     Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,3,0,0"/>
        </StackPanel>
      </DockPanel>
    </Border>

    <!-- 关键指标（间距 12px 卡间；ItemsControl 右 Margin -12 抵消最后一张卡的右间距，对齐右缘） -->
    <ItemsControl ItemsSource="{Binding Metrics}" Margin="0,18,-12,0">
      <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
          <UniformGrid Columns="3"/>
        </ItemsPanelTemplate>
      </ItemsControl.ItemsPanel>
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border Style="{DynamicResource CardBorder}" Margin="0,0,12,0">
            <StackPanel>
              <TextBlock Text="{Binding ValueText}" FontSize="18" FontWeight="Bold"
                         Foreground="{DynamicResource TextPrimaryBrush}"/>
              <TextBlock Text="{Binding Label}" FontSize="{DynamicResource FontSizeCaption}"
                         Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,3,0,0"/>
              <Grid Height="4" Margin="0,9,0,0">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="{Binding Fraction, Converter={StaticResource FracLen}}"/>
                  <ColumnDefinition Width="{Binding Fraction, Converter={StaticResource FracLen}, ConverterParameter=rest}"/>
                </Grid.ColumnDefinitions>
                <Border Grid.ColumnSpan="2" CornerRadius="2"
                        Background="{DynamicResource TrackBrush}"/>
                <Border Grid.Column="0" CornerRadius="2" HorizontalAlignment="Stretch"
                        Background="{Binding ColorKey, Converter={StaticResource KeyBrush}}">
                  <Border.Effect>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.45"
                                      Color="{Binding ColorKey, Converter={StaticResource KeyColor}}"/>
                  </Border.Effect>
                </Border>
              </Grid>
            </StackPanel>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>

    <!-- 详情区（渐进披露：默认收起） -->
    <Border Style="{DynamicResource CardBorder}" Margin="0,10,0,0"
            Visibility="{Binding DetailVisible, Converter={StaticResource BoolVis}}">
      <TextBlock Text="诊断详情将在后续阶段接入实时数据"
                 Foreground="{DynamicResource TextTertiaryBrush}"
                 FontSize="{DynamicResource FontSizeCaption}"/>
    </Border>

    <!-- 最近活动 + 快速诊断（填充下方空白，规格 §5.1 布局） -->
    <Grid Margin="0,18,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="18"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>

      <Border Grid.Column="0" Style="{DynamicResource CardBorder}">
        <StackPanel>
          <TextBlock Text="最近活动" FontSize="{DynamicResource FontSizeCaption}"
                     FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
          <Grid Margin="0,8,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock Text="竞技模式已启用" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
            <TextBlock Grid.Column="1" Text="刚刚" FontSize="11" Foreground="{DynamicResource TextTertiaryBrush}"/>
          </Grid>
          <Grid Margin="0,6,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock Text="系统体检完成" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
            <TextBlock Grid.Column="1" Text="2 分钟前" FontSize="11" Foreground="{DynamicResource TextTertiaryBrush}"/>
          </Grid>
          <Grid Margin="0,6,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock Text="游戏检测就绪" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
            <TextBlock Grid.Column="1" Text="已开启" FontSize="11" Foreground="{DynamicResource TextTertiaryBrush}"/>
          </Grid>
        </StackPanel>
      </Border>

      <Border Grid.Column="2" Style="{DynamicResource CardBorder}">
        <StackPanel>
          <TextBlock Text="快速诊断" FontSize="{DynamicResource FontSizeCaption}"
                     FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
          <Grid Margin="0,8,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock Text="GPU 驱动" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
            <TextBlock Grid.Column="1" Text="正常" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}"/>
          </Grid>
          <Grid Margin="0,6,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock Text="后台进程" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
            <TextBlock Grid.Column="1" Text="12 个" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
          </Grid>
          <Grid Margin="0,6,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock Text="反作弊监控" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
            <TextBlock Grid.Column="1" Text="运行中" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}"/>
          </Grid>
        </StackPanel>
      </Border>
    </Grid>
  </StackPanel>
</UserControl>
